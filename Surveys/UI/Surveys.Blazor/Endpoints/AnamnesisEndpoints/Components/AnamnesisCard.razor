@using Surveys.Blazor.Endpoints.QuestionEndpoints.Components
@using Surveys.Web.Application.Messaging.QuestionMessages.ViewModels
<Card>
    <CardHeader>
        <CardTitle>
            @(Anamnesis.AnamnesisTemplate?.Title ?? "Неизвестно")<Check Checked="@Anamnesis.IsComplete" Disabled="true"/>
        </CardTitle>
    </CardHeader>
    <CardBody>
        @if (Anamnesis.AnamnesisTemplate != null)
        {
            foreach (QuestionViewModel question in Anamnesis.AnamnesisTemplate.Questions)
            {
                <QuestionCard OnAnswerSaved="OnAnswerSaved" Question="@question"/>
            }
        }
    </CardBody>
</Card>